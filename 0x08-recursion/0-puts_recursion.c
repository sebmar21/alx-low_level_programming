#include <stdio.h>

/**
 * _puts - a function that print a string
 *         trailing new line
 *@s: input string
 *
 * Return: nonnegative number (Success)
 */
 
void _puts_recursion(char *s)
{
	int puts(char *s);
	return (0);
}

#include"main.h"

/**
 * is_palindrome - a function that returns 1
 *                 if a string is
 *                 a palindrome
 *@s: string
 *Return: Always 0 (Success)
 */

int is_palindrome(char *s)
{
	if (s = "")
		return (1);
	else
		return (0);
}

#include"main.h"

/**
 * is_prime_number - a function that returns 1
 *                   if the input integer
 *                   is a prime number
 *@n: input integer
 *Return: nothing
 */
int is_prime_number(int n)
{
	if (n > 1)
		return (1);
	else
		return (0);
}
